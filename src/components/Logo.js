import React from "react"

import logo from "../assets/logo.svg"

const Logo = (props) => {
    return (
        <div className={"logo"} >
            <img src={logo} alt="" />
        </div>
    )
}

export default Logo